import { RegisterView } from '@/sections/auth';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Register',
};

export default function RegisterPage() {
  return <RegisterView />;
}